---
title: "{{ title }}"
artifact: PRD-{{ number }}
status: {{ status | default("Draft") }}
author: {{ author }}
created: {{ created_date }}
last-updated: {{ last_updated_date }}
parent-epic: EPIC-{{ parent_epic_number }}
linked-research:
{%- for ref in linked_research | default([]) %}
  - {{ ref }}
{%- endfor %}
linked-adrs:
{%- for adr in linked_adrs | default([]) %}
  - {{ adr }}
{%- endfor %}
---

# {{ title }}

## Overview

{{ overview | default("What this PRD covers — scoped to something a team or agent can ship and validate independently.") }}

## Requirements

{{ requirements | default("Detailed functional and non-functional requirements.") }}

## Implementation Approach

{{ implementation_approach | default("High-level plan that seeds the implementation plan (via execution-tracking).") }}

## Lifecycle

| Phase | Date | Commit | Notes |
|-------|------|--------|-------|
| {{ status | default("Draft") }} | {{ created_date }} | {{ commit_hash }} | Initial creation |
